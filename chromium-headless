#!/bin/bash
# Headless Chromium wrapper that always disables the kernel user-namespace
# sandbox. Required because we run in non-privileged Docker, where the
# sandbox bootstrap fails and crashes the browser process on every CDP
# connect. The container is our isolation boundary; the in-browser sandbox
# is not load-bearing for that.
exec /usr/bin/chromium --no-sandbox "$@"
